# 기능 API 스펙

이번 변경은 API 엔드포인트 추가 또는 계약 변경이 없다.

기존 회원가입 / 로그인 / 토큰 재발급 API의 응답 구조는 동일하다.
Redis 장애 시 기존 `200` 대신 `500`이 반환될 수 있다는 정책이 추가된다.

## 에러 응답 추가

| 상황 | HTTP Status | 코드 | 메시지 |
|---|---|---|---|
| Redis 저장/조회 실패 | 500 | `AUTH-500` | 인증 처리 중 오류가 발생했습니다 |
