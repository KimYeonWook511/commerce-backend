# 태스크 API 스펙

## 변경 사항

이 태스크는 외부 API를 추가하거나 변경하지 않는다.

내부 Service 메서드 시그니처만 변경되며, Controller와 Request/Response DTO는 영향받지 않는다.

- `GET /payments/naverpay/approve` 응답 구조 변경 없음
- `POST /payments/naverpay/approve` 응답 구조 변경 없음
- `POST /payments/ready` 응답 구조 변경 없음
