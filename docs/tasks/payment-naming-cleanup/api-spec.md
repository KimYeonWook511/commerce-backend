# 태스크 API 스펙

## 개요

- 변경 없음. 순수 네이밍/구조 정리 작업이라 추가·변경·삭제되는 API 엔드포인트가 없다.

## 엔드포인트

- 해당 없음.

## 요청

- 해당 없음.

## 응답

- 해당 없음.
- 에러 응답의 `code` 문자열(`PAYMENT-404-2` 등)과 메시지는 그대로 유지된다. `PaymentErrorCode` 의 enum 식별자만 내부적으로 rename된다.

## 검증 규칙

- 해당 없음.

## 비고

- 외부 계약(요청/응답 스키마, 에러 code·메시지, HTTP status)은 일절 변경하지 않는다.
