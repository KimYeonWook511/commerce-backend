# 태스크 DB 스키마

## 개요

DB 스키마 변경 없음. 본 태스크는 application/infra 계층의 예외 처리 패턴 리팩터링이며, DB 테이블·컬럼·인덱스·제약 모두 그대로 유지한다. Redis 장애 시에도 기존 응답(500)을 그대로 반환하므로 DB 동작 차이 없다.

## 신규 테이블

없음.

## 변경 테이블

없음.

## 인덱스

기존 인덱스 그대로 유지.

## 데이터 무결성

변경 없음.

## 마이그레이션 고려사항

없음. 스키마 변경 없으므로 배포 순서·백필·롤백 이슈 없음.
