-- 옛 결제·예약 테이블을 버린다. 이 테이블을 읽고 쓰던 코드가 모두 사라져 남겨 두면
-- 쓰는 데 없는 테이블만 남는다.
--
-- [경고] 파괴적 마이그레이션 — 운영 데이터 없음(pre-prod) 전제.
-- 옛 결제·예약 데이터를 새 모델로 이관하지 않고 영구 폐기한다. 진행 중이던 건이 있으면 함께 사라진다.
-- 이관하지 않는 이유: 옛 코드는 결제사에 실어 보낸 환불 시도 키를 저장하지 않아, 옮겨 놓아도 결제사
-- 이력에서 우리 사건을 집어낼 수 없다.

DROP TABLE `tbl_legacy_payment_reservation`;

DROP TABLE `tbl_legacy_payment`;
