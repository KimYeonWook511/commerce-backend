package com.commerce.payment.domain;

/**
 * 누가 이 환불을 요청했나. 결제사 취소 요청의 필수 필드로 실어야 하고, 우리 쪽에서는 회원 요청과
 * 시스템 생성을 가른다.
 *
 * <p>환불 요청 멱등키의 유일 범위에 이 값이 함께 드는 것은 두 값의 신뢰 경계가 다르기 때문이다 —
 * 회원 값은 밖에서 오는 임의 문자열이고 시스템 값은 우리가 만든다. 같은 공간에 두면 회원이 시스템
 * 값과 같은 문자열을 보내 내부 제약을 건드릴 수 있다.
 */
public enum RefundRequester {
	MEMBER,
	SYSTEM
}
